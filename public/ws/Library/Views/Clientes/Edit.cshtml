@model Library.Models.Cliente

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Cliente</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="CLI_USUCVE" />
            <input type="hidden" asp-for="CLI_CLAVE" />
            <div class="form-group">
                <label asp-for="CLI_RFC" class="control-label"></label>
                <input asp-for="CLI_RFC" class="form-control" />
                <span asp-validation-for="CLI_RFC" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CLI_NOMBRE" class="control-label"></label>
                <input asp-for="CLI_NOMBRE" class="form-control" />
                <span asp-validation-for="CLI_NOMBRE" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
